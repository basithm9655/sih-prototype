[build]
  # Assuming index.html is in the root directory. 
  # If it's in a folder like "public" or "dist", change this.
  publish = "." 
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
